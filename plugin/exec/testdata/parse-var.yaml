name: parse-var
description: parsing variable save/restore functionality
tests:
  - exec: echo 42
    var-stdout: VAR_STDOUT
    var-stderr: VAR_STDERR
    var-rc: VAR_RC
    var:
      MY_ENVVAR:
        from: MY_ENVVAR
